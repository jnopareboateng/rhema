DEEPGRAM_API_KEY="your_key_here"

# Shared secret for the dev Bible bridge (browser dev mode).
# Generate: openssl rand -hex 16
# Also set DEV_BRIDGE_TOKEN=<same value> when starting the bridge.
VITE_DEV_BRIDGE_TOKEN="your_token_here"
